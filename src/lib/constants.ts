/** Primary origin for all SEO absolute URLs (canonical, hreflang, OG).
 *  Always points to Cloudflare Pages regardless of build environment. */
export const PRIMARY_ORIGIN = 'https://bitcoin-institute.pages.dev';
